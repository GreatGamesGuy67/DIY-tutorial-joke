/**
 * vars.js
 * Central site configurations and theme definitions
 */

window.SiteConfig = {
    // Total number of tutorials currently active in your main/ directory
    maxTutorials: 4,
    
    // Aesthetic color schemes used for UI borders and text rendering
    themeColors: {
        accentPink: "#ff3366",
        accentCyan: "#00ffff",
        accentGreen: "#00ffcc"
    }
};
